.DS_Store
Thumbs.db
desktop.ini
*.tmp
~$*

# Keep full local dataset, but do not upload all RGB-D frames to GitHub.
# Force-added: decision-GT val frames (220) + one sample frame per crop in train/.
rgbd/
